version: "3.9"

services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=calendar
      - POSTGRES_PASSWORD=calendar
      - POSTGRES_DB=calendar
    ports:
     - "5432:5432"
    networks:
      - calendar-net

networks:
  calendar-net:
